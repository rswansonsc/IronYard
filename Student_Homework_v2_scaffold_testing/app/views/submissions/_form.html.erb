<%= form_for(@submission) do |f| %>
  <% if @submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submission.errors.count, "error") %> prohibited this submission from being saved:</h2>

      <ul>
      <% @submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_s %><br>
    <%= f.text_field :name_s %>
  </div>
  <div class="field">
    <%= f.label :descripion_s %><br>
    <%= f.text_field :descripion_s %>
  </div>
  <div class="field">
    <%= f.label :links_s %><br>
    <%= f.text_field :links_s %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
   <%= link_to 'Home', root_path %>
<% end %>
